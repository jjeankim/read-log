import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["search1.kakaocdn.net", "search2.kakaocdn.net", "t1.daumcdn.net"],
  },
};

export default nextConfig;
